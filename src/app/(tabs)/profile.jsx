import { SafeAreaView } from 'react-native';
import Header from '../../components/Header';

const Profile = () => {
  return (
   <SafeAreaView>
      <Header title={"umesh +"}/>
    </SafeAreaView>
  )
}

export default Profile;